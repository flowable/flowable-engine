<?xml version="1.0" encoding="UTF-8" ?> 

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
  
<mapper namespace="org.flowable.common.engine.impl.persistence.change.ChangeLog">

  <!-- CHANGELOG SELECT -->
  
  <select id="selectAppChangeLogVersions" resultType="string">
    select ID from ${prefix}ACT_APP_DATABASECHANGELOG order by ORDEREXECUTED
  </select>
  
  <select id="selectCmmnChangeLogVersions" resultType="string">
    select ID from ${prefix}ACT_CMMN_DATABASECHANGELOG order by ORDEREXECUTED
  </select>
  
  <select id="selectDmnChangeLogVersions" resultType="string">
    select ID from ${prefix}ACT_DMN_DATABASECHANGELOG order by ORDEREXECUTED
  </select>
  
  <select id="selectEventRegistryChangeLogVersions" resultType="string">
    select ID from ${prefix}FLW_EV_DATABASECHANGELOG order by ORDEREXECUTED
  </select>
  
  <select id="selectFlowableChangeLogVersions" parameterType="string" resultType="string">
    select ID from ${prefix}FLW_#{name, jdbcType=VARCHAR}_DATABASECHANGELOG order by ORDEREXECUTED
  </select>
  
</mapper>
